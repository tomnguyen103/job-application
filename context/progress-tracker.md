# Progress Tracker

Update this file after every completed feature. Any AI agent reading this should immediately know what is done, what is in progress, and what is next.

---

## Current Status

**Phase:** Grade-A Hardening
**Last completed:** PR 2 Billing correctness + billing tests
**Next:** PR 3 Reliability: Gemini timeouts and failure isolation

---

## Progress

### Phase 1 - Foundation

- [x] 01 Homepage
- [x] 02 Auth
- [x] 03 PostHog Initialization
- [x] 04 Database Schema

### Phase 2 - Profile Page

- [x] 05 Profile Page - Full UI
- [x] 06 Profile Save Logic
- [x] 07 AI Profile Extraction from Resume
- [x] 08 Resume PDF Generation from Profile

### Phase 3 - Find Jobs Page

- [x] 09 Find Jobs Page - Full UI
- [x] 10 Adzuna Job Discovery
- [x] 11 Filter + Sort + Pagination

### Phase 4 - Job Details Page

- [x] 12 Job Details Page - Full UI
- [x] 13 Company Research Agent

### Phase 5 - Dashboard

- [x] 14 Dashboard Page - Full UI
- [x] 15 Stats Bar - Real Data
- [x] 16 Recent Activity - Real Data
- [x] 17 Analytics Charts - PostHog Data

### Phase 6 - Tailored Resume

- [x] 18 Job-Tailored Resume Agent

### Website Modernization

- [x] Phase 1 - Visual Baseline And Design System Refresh
- [x] Phase 2 - Homepage Redesign
- [x] Phase 3 - Login And Public Exploration
- [x] Phase 4 - Authenticated Workspace Polish
- [x] Phase 5 - Engagement Features Without Billing
- [x] Phase 6 - Stripe SaaS Integration
- [x] Luxury Homepage Redesign

---

## Decisions Made During Build

- 2026-07-01: Grade-A hardening PR 2 completed the billing correctness batch. The non-find quota routes now check `recordUsage`/quota verdicts and return the shared 402 quota payload when billing denies usage; research, base resume generation, tailored resume generation, and resume extraction reserve through the atomic `record_usage_with_quota_check` RPC before expensive work, with resume extraction releasing its recent route-specific reservation through `release_resume_extract_reservation` when parsing throws or returns no data. `getPeriodBoundaries` now mirrors the SQL guard by trusting Pro billing dates only when both dates parse and `periodStart < periodEnd`. Billing webhook handling now reclaims stale `pending` and `failed` idempotency rows, keeps already-processed duplicates idempotent, checks the failed-event status update, and uses narrow structural client/event types instead of `any`. Added real quota helper coverage, entitlement grace-period boundary tests, and webhook branch coverage for subscription created/updated/deleted, invoice paid/payment failed, processed duplicates, active pending guards, failed/stale duplicate reclaim, and metadata user fallback. Local verification: `npm ci`, `npm run typecheck`, `npm run lint`, `npm test` (121/121), `npm run build`, and `git diff --check` all passed.
- 2026-07-01: Grade-A hardening PR 1 started from `docs/grade-a-goal-prompt.md`. Added GitHub Actions CI at `.github/workflows/ci.yml` running `npm ci`, `npm run typecheck`, `npm run lint`, `npm test`, and `npm run build` on push and pull requests with public build-time env stubs only, pinned to the repo-documented Node 20 runtime. Added the `typecheck` package script and widened the test pattern to `tests/**/*.test.ts` through `scripts/run-tests.mjs` so nested test files are not silently skipped and Node 20 CI does not depend on newer Node glob expansion. Local verification before PR: `npm ci`, `npm run typecheck`, `npm run lint`, `npm test` (106/106), `npm run build`, and `git diff --check` all passed.
- 2026-07-01: Updated Remotive search after user review: no hardcoded local city/country gate. `agent/job-sources/remotive.ts` now builds the documented Remotive API query from the Find Jobs text-field inputs by sending both `jobTitle` and `location` through the public API's `search` parameter, plus the app's requested `limit`. Live probing showed Remotive's public API can still return broad remote-region rows for that query, so normalized Remotive rows are also filtered by comparing each returned `candidate_required_location` to tokens from the user's Location text; this keeps the behavior input-driven instead of keyed to Houston or any fixed location list. Cleaned up 10 already-saved Remotive rows from prior Houston searches so the table no longer keeps showing stale broad-region results. Updated `docs/job-search-api-setup-guide.md` with the Remotive API limitation, the exact query shape, and the verification expectation.
- 2026-07-01: CodeRabbit follow-up after PR #19 merge. Updated the latest `record_usage_with_quota_check` migration definition and added live migrations `20260701203000_prevent-usage-rpc-quota-overflow.sql` and `20260701204500_avoid-usage-rpc-addition-overflow.sql` so usage totals use `bigint` and the quota guard compares against the remaining allowance instead of adding before the denial path can return.
- 2026-07-01: Fixed a production-only Find Jobs outage. Vercel runtime logs showed `POST /api/agent/find` returning 500 before discovery because `recordUsage` required `INSFORGE_ADMIN_API_KEY`, which was present locally but missing in Vercel production. The quota path now calls an authenticated `SECURITY DEFINER` `record_usage_with_quota_check` RPC through the normal server client instead of requiring an admin API key in web runtime env. The RPC validates owner scope, positive quantities, non-empty idempotency keys, and takes the advisory lock before idempotency/quota checks. Find Jobs records one `job_search_run` when the run starts, then records one `job_match_score` immediately before each Gemini scoring call, avoiding any user-callable quota-reduction RPC.
- 2026-07-01: CodeRabbit follow-up on PR #19. Removed the pre-merge `release_reserved_usage` / `adjust_reserved_usage` RPC design because owner-scoped reduction functions would let an authenticated user lower their own usage ledger. Added a live cleanup migration that drops those functions from the already-migrated backend and replaces `record_usage_with_quota_check` with the final guarded version.
- 2026-07-01: Second CodeRabbit follow-up on PR #19. Moved `job_match_score` reservation inside each scoring task so usage is recorded immediately before the Gemini call, and added `record_usage_with_quota_check` validation for null/negative limits plus missing or inverted billing periods. Added a live migration to apply that final RPC shape to the already-migrated backend.
- 2026-07-01: Final CodeRabbit follow-up on PR #19. The usage RPC no longer trusts caller-supplied quota limits or billing windows; it derives the active plan, quota limit, and billing period from `user_entitlements` inside the database, schema-qualifies `public.usage_ledger`/`public.user_entitlements`, and sets `search_path = public, pg_temp`. Applied live migrations `20260701193000_derive-usage-rpc-quota.sql`, `20260701194500_restore-compatible-usage-rpc-signature.sql`, `20260701195500_add-usage-rpc-compat-wrapper.sql`, and `20260701200500_add-legacy-positional-usage-rpc-wrapper.sql` before deployment so current production callers, positional 7-arg callers, legacy positional quota callers, and the new PR code can record Find Jobs usage without Vercel admin env.
- 2026-07-01: CodeRabbit null-handling follow-up on PR #19. Updated every `record_usage_with_quota_check` migration definition to reject NULL `p_user_id` safely with `auth.uid() IS DISTINCT FROM p_user_id`, coalesced legacy wrapper metadata to `{}` instead of forwarding SQL NULL, and added live migration `20260701201500_harden-usage-rpc-null-handling.sql` so the already-applied production RPCs receive the same hardening.
- 2026-07-01: CodeRabbit follow-up on PR #17. `adjustReservedUsage` now true-ups quota with one guarded down-only update (`quantity > actual`) instead of a read-then-write sequence, so concurrent true-ups cannot raise a reservation. `resolveSourceDisplayName` now maps `adzuna` explicitly and renders invalid or missing providers as "Unknown source" instead of silently relabeling them as Adzuna; added direct tests for the provider display-name helper.
- 2026-07-01: CodeRabbit follow-up on the reservation true-up path. `releaseReservedUsage` now returns a boolean instead of swallowing delete failures, and zero-quantity `adjustReservedUsage` reports `{ success: false }` if the reservation cannot be released. Added regression coverage for the release-failure path.
- 2026-07-01: Follow-up hardening after the security/quality fix pass. Normalized `app/api/resume/generate/route.ts` and `app/api/resume/download/route.ts` JSON error responses to include `{ success: false, error }`, matching the documented API route response envelope while preserving PDF streaming success responses. Added actual `route.ts` coverage for `/api/billing/webhook` rejection paths (unsigned request, forged signature, signed invalid JSON) so signature enforcement is tested at the route boundary, not only as a helper. Updated the `CompletionIndicator` test to render through React server rendering instead of calling the component as a plain function.
- 2026-07-01: Full-project reassessment (5-agent review across code quality, security, test coverage, architecture boundaries, and performance) followed by a fix pass covering every finding, applied on top of the previously-uncommitted multi-source job discovery feature. CRITICAL: `app/api/billing/webhook/route.ts` accepted any POSTed body as a genuine Stripe event with zero signature verification — anyone who found the URL could forge entitlement upgrades or downgrades for any user. Fixed with new `lib/billing/verify-webhook-signature.ts`, a dependency-free HMAC-SHA256 verification of the `Stripe-Signature` header using Node's built-in `crypto` (no new package — matches the project's "simpler native solution first" dependency rule), including timestamp-tolerance replay protection. `STRIPE_WEBHOOK_SECRET` added to `.env.example`/code-standards.md; must be set from the Stripe Dashboard/CLI before this endpoint can process real traffic — billing is not production-safe until then. CRITICAL: `app/api/agent/find/route.ts` checked quota with a plain SELECT and only recorded usage via the atomic `record_usage_with_quota_check` RPC after the run finished, so concurrent searches from the same user could each pass a stale check and jointly exceed quota (extra Gemini cost, jobs saved past the plan limit). Fixed by moving both `job_search_run` and `job_match_score` reservations to atomic `recordUsage` calls *before* `discoverJobs` runs at all, hard-capping scoring to whatever was actually reserved, releasing reservations if a later step fails, and truing the `job_match_score` reservation down to the real `result.saved` count afterward via new `releaseReservedUsage`/`adjustReservedUsage` helpers in `lib/billing/usage.ts`. Also closed a compounding PostgREST `.or()` filter-injection risk in the webhook's subscription-lookup query by validating `customer`/`subscription` IDs against Stripe's own ID shape before interpolating them.
- 2026-07-01: Job-source adapter hardening. `agent/job-sources/{greenhouse,lever,ashby}.ts` now fall back to "Not specified" instead of an empty string when a board omits location (caught by new normalizer tests — previously rendered a blank Location cell). The same three adapters now fetch multiple configured boards concurrently via a new `searchAtsBoards` helper in `agent/job-sources/utils.ts` instead of a serial per-board loop (inert today with the default single-board config, but was a latent N-times latency multiplier). `agent/job-sources/usajobs.ts` now slices to `input.limit` like every other adapter instead of relying solely on the server honoring `ResultsPerPage`. `agent/job-discovery.ts` now logs a summary `agent_logs` entry when every enabled source fails, matching the existing success-path logging. Added normalization unit tests for Greenhouse/Lever/Ashby (previously zero coverage on the three ATS adapters).
- 2026-07-01: Housekeeping from the same review. Deleted `agent/adzuna.ts` (a compatibility re-export with zero remaining importers after the multi-source migration) and its `architecture.md` folder-tree entry. Added try/catch wrapping the full handler body in `app/api/resume/generate/route.ts` and `app/api/resume/download/route.ts` (both previously left part of their body outside any catch, violating the documented route-handler pattern). Renamed `NEXT_PUBLIC_STRIPE_PRO_PRICE_ID` to server-only `STRIPE_PRO_PRICE_ID` (the price ID never needed to reach the browser) and added a `STRIPE_ENVIRONMENT` env var (defaults to `test`, matching InsForge's payments guidance to stay on test until the developer explicitly approves live traffic) in place of a hardcoded `"test"` literal. Trimmed a full-request PII log in `handlePortal` down to a non-identifying field. Added `*.db` to `.gitignore` (an untracked local `ruvector.db` tooling artifact, unrelated to the app's InsForge backend, was not previously excluded). DEFERRED (flagged, not fixed — out of scope for this pass): webhook rate limiting, and full component-render/E2E test coverage (this project's tests are all pure-function/logic-level; no React render harness or Playwright/Cypress exists yet). `npm test` 96/96 (11 new), lint and production build green.
- 2026-06-29: Polished the Job Details interview prep marker treatment for both light and dark themes. Section markers now use a semantic-token outer ring with an inner tone dot, and list bullets inherit each section's tone so the markers read intentionally without changing the current UI theme.
- 2026-06-29: Updated the Find Jobs saved listings table to include each job's Location immediately after Match Score while preserving the existing semantic-token theme. The server page now selects `jobs.location`, `JobListItem` carries it into `JobsTable`, desktop renders a 6-column table, and mobile cards show Location directly below the match meter.
- 2026-06-29: Redesigned the public homepage as a premium "private career desk" experience while preserving the core business purpose: find relevant roles, research companies, and tailor resumes. Added semantic premium material tokens (`chrome`, `ink`, `dossier-line`, `dossier-wash`, `shadow-artifact`), rebuilt homepage sections around the Career Dossier product artifact using the existing dashboard/jobs/agent-log assets, tightened public copy, kept top-navbar IA unchanged, and updated the UI registry/token docs. Verification target: desktop and mobile browser screenshots plus lint, typecheck, tests, and production build.
- 2026-06-08: Built Feature 01 as a static App Router homepage matching `context/designs/landing-page.png`, using the provided public assets for the logo, dashboard preview, jobs list, agent log, and testimonial avatar.
- 2026-06-08: Primary and header Get Started links point to `/login`; Find Your First Match links point to `/find-jobs` until auth-aware routing is implemented in Feature 02.
- 2026-06-08: Built Feature 02 with InsForge OAuth for Google and GitHub using `@insforge/sdk/ssr`, server-side PKCE callback exchange, `/api/auth/refresh`, and Next.js 16 `proxy.ts` route protection.
- 2026-06-08: Applied InsForge auth redirect config for local OAuth callbacks: `http://localhost:3000/api/auth/callback` and `http://127.0.0.1:3000/api/auth/callback`.
- 2026-06-08: Added minimal protected workspace shells for `/dashboard`, `/profile`, and `/find-jobs` so post-login redirects and auth protection have concrete route targets.
- 2026-06-08: Tightened Feature 02 after review: OAuth provider buttons now render only from live InsForge public auth config, the login page shows a safe unavailable state when no supported providers are enabled, and OAuth submissions show a pending state.
- 2026-06-08: Built Feature 03 with guarded PostHog browser/server initialization, manual pageview capture, typed helpers for the four approved product events, and client-side identity sync after OAuth login/signout.
- 2026-06-09: Fixed the Feature 03 review finding on logout identity reset. Added `components/layout/SignOutButton.tsx` — a small client component that calls `resetPostHog()` on click before submitting the existing `signOut` server action. This resets PostHog identity immediately on logout instead of relying on the mount-only effect in `PostHogProvider.tsx`, which does not re-run on the soft navigation triggered by the `signOut` redirect. `Navbar.tsx` stays a Server Component. `npm run lint` and `npm run build` pass.
- 2026-06-09: Built Feature 04 (Database Schema) via InsForge CLI migration `migrations/20260609064130_create-core-schema.sql`, applied with `db migrations up --all`. Created `profiles`, `agent_runs`, `jobs`, `agent_logs` with columns per architecture.md. 16 RLS policies (SELECT/INSERT/UPDATE/DELETE × 4 tables), owner-scoped: `profiles` on `id = auth.uid()`, the other three on `user_id = auth.uid()`. Verified tables, policies, and indexes against the live backend.
- 2026-06-09: FK targets — all `user_id` columns and `profiles.id` reference `auth.users(id)` with `ON DELETE CASCADE` (InsForge convention from AGENTS.md/skill: FK to `auth.users(id)`, `auth.uid()` in RLS), not `profiles`. Avoids a creation-order dependency and matches the platform.
- 2026-06-09: Enforced documented invariants as CHECK constraints — `source` ('search'|'url'), `agent_runs.status`, `agent_logs.level`, the profile dropdowns (`experience_level`, `remote_preference`, `cover_letter_tone`, `work_authorization`, NULL-allowed since the profile starts empty), and `match_score` 0–100.
- 2026-06-09: DEVIATION from the confirmed plan — left `jobs.job_type` as free text (no CHECK). Adzuna's `contract_type` vocabulary (e.g. `permanent`) does not match the documented `fulltime/parttime/contract` enum and library-docs.md maps it straight through; a CHECK would break Feature 10 ingestion. The "tailored fields" from the build plan were excluded (out of scope; not in architecture.md schema).
- 2026-06-09: No signup trigger — the `profiles` row is created by upsert on first save (Feature 06). Feature 04 is structure-only; `updated_at` is set by the save action, not a DB trigger. Indexes added for known access patterns: `jobs(user_id, match_score DESC)`, `jobs(user_id, found_at DESC)`, `jobs(run_id)`, `agent_runs(user_id, started_at DESC)`, `agent_logs(run_id)`, `agent_logs(user_id)`.
- 2026-06-09: Created private `resumes` storage bucket (`public=false`). InsForge storage authorization is API-level (bucket public/private + `storage.objects.uploaded_by`), NOT Postgres RLS — `storage.objects` ships with RLS disabled, zero policies, and there is no storage-policy CLI command. Did NOT add RLS to `storage.objects` (would fight the platform and risk breaking storage). Own-files isolation = private bucket + per-user path `resumes/{user_id}/resume.pdf` + server-side access scoped to the current user. Feature 06 must always use the current user's id in the storage path and never a user-supplied path.
- 2026-06-09: Built Feature 06 (Profile Save Logic). New files: `lib/utils.ts` (`MATCH_THRESHOLD`, `computeProfileCompletion`), `actions/profile.ts` (`saveProfile` and `saveResume` Server Actions). Modified: `app/profile/page.tsx` (replaces mock data with live DB read via `insforge.database.from("profiles").maybeSingle()`), `components/profile/ProfileForm.tsx` (wired to `saveProfile` via `<form action>` + `useActionState`; work-experience roles are now controlled so hidden JSON inputs stay in sync), `components/profile/ResumeUpload.tsx` (wired to `saveResume`; accepts `resumeUrl` prop; file submit via `form.requestSubmit()`). `profile_completed` PostHog event fires on first save that crosses `is_complete = true`. Also corrected `context/library-docs.md` InsForge DB/Storage examples to match the real `@insforge/sdk` v1.3.1 API (`database.from`, `upload(path, File|Blob)` no options arg, `getPublicUrl` returns string). `npm run lint` and `npm run build` both pass.
- 2026-06-09: Built Feature 05 (Profile Page — Full UI) to match `context/designs/profile.png`. New files: `app/profile/page.tsx` (replaced the stub Server Component), `components/profile/CompletionIndicator.tsx`, `components/profile/ResumeUpload.tsx`, `components/profile/ResumePreview.tsx`, `components/profile/ProfileForm.tsx`, and `types/index.ts` (`Profile`/`WorkExperience`/`Education` types). Mock data only, no save logic — the Save Profile and Generate Resume buttons are inert (Feature 06/08 wire them). `npm run lint` and `npm run build` both pass (TypeScript clean; `/profile` is a dynamic route).
- 2026-06-09: Profile page uses a stacked-card layout on the grey page background (`max-w-[1080px]` centered, `flex flex-col gap-6`), NOT the bordered white-surface workspace shell used by the dashboard/find-jobs stubs — this matches the floating-cards design. Page is a Server Component that holds the mock `Profile` object and passes it as props to the form components, so Feature 06 swaps mock → real DB data with the same prop shape.
- 2026-06-09: No icon library is installed (lucide-react is on the approved list but not in package.json), so all profile icons are inline SVG with `stroke="currentColor"` + a token text-color class. Kept hand-built components (no shadcn/ui — none is installed) consistent with Features 01–03. Interactivity is client-side local state only (skill/industry tags add/remove, add/remove up to 3 work-experience roles, "Currently working here" disables End Date) — nothing persists.
- 2026-06-09: Added hand-written token utilities to `app/globals.css` for Feature 05 (`.bg-accent`, `.bg-accent-muted`, `.text-error`, `.text-border-light`, `.checkbox-accent`, plus `placeholder:`/`focus:`/`disabled:` variants). This follows the existing project pattern of explicitly defining token utilities and their variants because Tailwind v4 `@theme` auto-generation is unreliable here.
- 2026-06-09: Built Feature 08 (Resume PDF Generation from Profile). New files: `agent/generator.ts` (Gemini `gemini-2.5-flash` via `@google/genai`, temperature 0.7, returns sanitized `{ professionalSummary, roles[].bullets[] }`), `app/api/resume/generate/ResumeDocument.tsx` (colocated react-pdf template — A4, built-in Helvetica, print-document color constants since a PDF cannot consume CSS variables), `app/api/resume/generate/route.ts` (POST: 401 unauth → 400 if `is_complete` is not true → 422 if Gemini polish fails, no fallback render → `renderToBuffer` → wrap in `File` → storage `remove` + `upload` → `resume_pdf_url` upsert → `revalidatePath("/profile")`). Modified: `components/profile/ResumeUpload.tsx` (Generate button wired — spinner pending state, disabled during upload/extract, inline error/success lines, `router.refresh()` on success so the refreshed `resumeUrl` shows the preview). Installed `@react-pdf/renderer`. `npm run lint` and `npm run build` pass.
- 2026-06-09: F08 design rules — AI polishes narrative content ONLY (summary paragraph + experience bullets); facts (name, contact, companies, titles, dates, education, skills) render verbatim from the profile so nothing can be hallucinated. Generation reads the last-SAVED profile from DB (unsaved form edits are not included) and replaces the active profile resume at `resumes/{user_id}/resume.pdf` (one active profile resume per user). No PostHog event — resume generation is not in the approved four-event list.
- 2026-06-09: Refactored the profile row→`Profile` mapping into shared `mapProfileRowToProfile` in `lib/utils.ts` with a `ProfileRow` type in `types/index.ts`; `app/profile/page.tsx` and the generate route both use it (single source for DB→app mapping).
- 2026-06-09: react-pdf quirk — a unitless `lineHeight` set at the `Page` level inherits into child `Text` as an ABSOLUTE length computed from the page `fontSize`, so large text (the 22pt name) got a ~13pt line box and overlapped the next line. Fix: give any text larger than the page font its own `lineHeight` (name uses `1.15`). Caught by rendering a sample PDF and inspecting it visually.
- 2026-06-09: Fixed stale `@react-pdf/renderer` example in `context/library-docs.md` — InsForge storage `upload()` has NO `upsert` option and takes `File | Blob` only; the documented pattern is wrap the buffer in a `File`, then `remove` + `upload` (matches the corrected InsForge section).
- 2026-06-09: F08 verification — lint/build green; template visually verified via a temporary tsx render script (sample PDF reviewed, layout bug found and fixed); unauthenticated POST against the running dev server returned 401 as designed, proving the route module graph loads in the real Next runtime. The full authenticated path (real Gemini polish + storage write + UI refresh) still needs a logged-in manual test: complete profile → Generate Resume from Profile → preview appears.
- 2026-06-09: Post-review fixes for Feature 08. User testing surfaced the core gap: the resume was invisible — no view affordance existed anywhere, and `resume_pdf_url` stores a private-bucket display URL that cannot authorize a plain browser GET (per InsForge docs the URL is display-only; downloads must go through an authenticated client). Fixes: (1) new `GET /api/resume/download` route — session-scoped `storage.download("{user_id}/resume.pdf")` streamed back with `Content-Disposition: inline` and `Cache-Control: private, no-store`, 404 when no file; uses the deterministic own-user path (same as extract) per the F04 own-files invariant. (2) `ResumePreview` file block is now a link to that route (`target="_blank"`) plus an explicit accent "View" link beside Replace. (3) Post-generate feedback: row filename switches to `resume.pdf` (fixes the stale uploaded-filename bug), subtitle becomes "Generated from your profile", success line says to click the file to view it. (4) The always-empty `fileSize` line is replaced by a `meta` subtitle — real size for fresh uploads (captured client-side at selection), "PDF document" for DB-loaded resumes. (5) Migration `20260609201614_add-resume-pdf-key.sql` (applied) adds `profiles.resume_pdf_key`; `saveResume` and the generate route now persist `uploadData?.key ?? path` alongside the url, per InsForge save-both-url-and-key guidance. Lint/build green; unauthenticated GET on the running dev server returns 401. Authed click-through (View opens the PDF; generate updates the row) needs a logged-in manual check.

- 2026-06-09: Built Feature 09 (Find Jobs Page — Full UI) matching `context/designs/find-jobs.png` exactly, per explicit user instruction. New files: `components/find-jobs/SearchControls.tsx` (search card: JOB TITLE input with search icon, LOCATION input, accent Find Jobs button, green success banner from a `successMessage` prop), `components/find-jobs/JobFilters.tsx` (filter bar: borderless inline search input + two pill-select dropdowns — All/High/Low Match and Match Score/Newest/Oldest, the F11 option sets), `components/find-jobs/JobsTable.tsx` (5-column table + exported `JobListItem` type), `components/find-jobs/JobsPagination.tsx` (results summary + Previous/pages/ellipsis/Next). Rewrote `app/find-jobs/page.tsx` (mock rows from the design, stacked cards `max-w-[1280px]`, no Footer — matches the profile authed-shell pattern). Everything is a Server Component with uncontrolled, inert controls; Features 10–11 wire the logic. `npm run lint` and `npm run build` pass; page visually verified logged-in against the design via the running dev server.
- 2026-06-09: F09 DEVIATIONS (design wins over docs, per "exactly as shown in the png"): (1) No SOURCE column — build-plan.md listed one, the design has 5 columns only. (2) Match-bar color mapping derived from the design — `>= 90` green, `80–89` blue, below orange — contradicting both ui-rules.md (80+ green) and ui-tokens.md (70–89 green); the design rows (94/96/91 green, 88/85 blue, 72 orange) are unambiguous. (3) Active nav item shows a `bg-accent` underline — ui-rules.md says color-change only, but the design clearly shows the indicator. Bar height stays 4px per ui-rules (png ambiguous at that scale). "Jobs by Adzuna" credit deferred to Feature 10 (not in the design; becomes relevant with real Adzuna data).
- 2026-06-09: App-wide rename JobPilot → "Job Application" (explicit user instruction). `public/logo.png` has the old wordmark baked into the image, so the brand is now code-rendered: new `components/layout/Logo.tsx` (gradient mark via `.brand-logo-mark` + 19px/700 "Job Application" text) used by Navbar, Footer, and the login brand panel; `/logo.png` is no longer rendered anywhere. Copy, alt text, and aria-labels renamed in Hero, JobSearchEase, ApplicationConfidence, and the login page; `metadata.title` is now "Job Application" (was "JobApplication").
- 2026-06-09: New `components/layout/NavLinks.tsx` (Client Component, `usePathname`) gives the navbar active-route highlighting (accent color + underline + icons + `aria-current`), resolving the cross-cutting polish item deferred since Feature 05. Navbar stays a Server Component.
- 2026-06-09: F09 follow-up (user request) — separated the JobFilters dropdowns from the text search: the filter row no longer wraps everything in one card; the text filter is a standalone `rounded-xl` bordered input (flex-1) and the two pill selects are independent elements beside it, all with `shadow-card`. Lint green; visually verified on the running dev server.
- 2026-06-09: Tooling note — the user's running dev server kept serving stale CSS after `globals.css` was edited (new plain-CSS rule `.brand-logo-mark` missing from the compiled chunk while auto-generated utilities appeared; the logo mark rendered invisible). A content nudge to `globals.css` (trailing newline) forced the Turbopack recompile and fixed it. If a new globals.css rule does not show up in the browser, touch the file rather than debugging the rule itself.

- 2026-06-09: Built Feature 10 (Adzuna Job Discovery). New files: `agent/types.ts` (`AdzunaJob`/`UsableAdzunaJob`/`JobMatchContent`/`DiscoveryResult`), `lib/adzuna.ts` (`searchJobs` — `category=it-jobs`, `results_per_page=10`; `detectCountry` keyword sniff → us/gb/au/ca), `agent/matcher.ts` (`scoreJobMatch` — Gemini 2.5-flash, temperature 0.3, F07/F08-style JSON-regex parse + strict sanitize with clamped integer score), `agent/adzuna.ts` (`discoverJobs` orchestrator + `agent_logs` writer), `app/api/agent/find/route.ts` (POST: 401 unauth → body validate 400 → `is_complete` gate 400 → `agent_runs` insert → discover → finalize run → `job_found` server events → `{ success, data: { found, saved, strongMatches } }`). Modified: `components/find-jobs/SearchControls.tsx` (now a Client Component — controlled inputs, in-button spinner, inline error, success banner fed by real counts, fires `job_search_started`, `router.refresh()` on success), `app/find-jobs/page.tsx` (real `jobs` rows: match_score desc, first 20, `{ count: "exact" }` totals), `components/find-jobs/JobsTable.tsx` (`JobListItem` gains `id`, empty-state row, pagination hidden at zero results), `lib/utils.ts` (`formatRelativeTime`). Lint + build green.
- 2026-06-09: F10 decisions — Gemini 2.5-flash for job scoring despite earlier stale architecture/library wording (build plan + F07/F08 precedent; direct app AI calls use Gemini). ALL scored jobs are saved regardless of score (low matches power F11's Low Match filter); the banner's "strong matches" = newly saved jobs with `match_score >= MATCH_THRESHOLD`. Repeat searches skip jobs whose `source_url` already exists for the user (never re-scored or re-inserted). A failed per-job scoring call skips that job with an `agent_logs` warning — the run continues, and no null-score row is ever written. Scoring runs in batches of 3 via `Promise.allSettled`. `agent_runs.jobs_found` stores the newly-saved count. Search is gated on `is_complete = true` (same gate as resume generation).
- 2026-06-09: F10 field findings — Adzuna's `where` parameter is strictly geographic: `where=remote` returns 0 results (live-verified), so `searchJobs` strips remote markers ("remote", "work from home", "wfh") plus stray punctuation and omits `where` entirely when nothing geographic remains (0 → 10 results after the fix). Adzuna can return `salary_min === salary_max`, so `formatSalary` collapses equal rounded bounds to a single value ("$146k") and renders min-only as "$146k+". `.env.local` had the Adzuna credentials misspelled `AZUNA_*` — renamed to the documented `ADZUNA_APP_ID`/`ADZUNA_APP_KEY`.
- 2026-06-09: F10 verification — lint/build green; live Adzuna smoke test via a temporary tsx script (credentials verified, 10 results returned, remote fix confirmed, response shape matches `AdzunaJob`); `detectCountry` unit checks pass; unauthenticated POST `/api/agent/find` → 401 against the running dev server. Pending: logged-in click-through (search → spinner → banner with real counts → table swaps to saved rows).
- 2026-06-09: F10 post-review fixes. User reported "search text field not working" after their click-through — triage showed the filter-bar text field (`JobFilters`) is inert BY DESIGN until Feature 11; the actual search path was confirmed working via live `agent_runs`/`agent_logs`. The review surfaced a REAL bug instead: Adzuna's `redirect_url` carries a per-request `?se=` tracking token, so dedupe-by-`source_url` never matched — the user's two identical searches saved 10 jobs TWICE (20 rows / 10 distinct, live-verified). Fix: `canonicalSourceUrl()` in `lib/adzuna.ts` (origin + path = stable ad id); dedupe and the stored `source_url` use the canonical form, while `external_apply_url` keeps the full tracking URL for apply clicks. One-time live-DB cleanup: canonicalized stored `source_url`s, then deleted later duplicates keeping the earliest row per (user_id, source_url) — verified 20 → 10. `agent_runs` history intentionally untouched (run 2 still records jobs_found=10 as history).
- 2026-06-09: F10 perf fix — runs took ~65s (Gemini 2.5-flash default THINKING dominated each scoring call at ~15s, × sequential batches of 3). Now: all ≤10 jobs score in a single `Promise.allSettled` (same request count per run, so rate-limit exposure is unchanged) and `agent/matcher.ts` sets `thinkingConfig: { thinkingBudget: 0 }` — scoring is structured extraction and needs no thinking. Real-call smoke test after the change: 1.3s per call (~10x faster) with output quality intact (sensible 65% score, correct matched/missing skills). Expected full run: well under 15s. Lint/build green.
- 2026-06-09: Built Feature 11 (Filter + Sort + Pagination) — Phase 3 complete. URL search params (`?q=&match=&sort=&page=`) are the single source of truth; the Server Component page parses and validates them (invalid values fall back to defaults) and builds ONE InsForge query: `.or("company.ilike.%term%,title.ilike.%term%")` for text search (the SDK supports PostgREST `or()`; the term is sanitized — `,()"%_\` stripped — because it is embedded in filter syntax), `gte`/`lt` `MATCH_THRESHOLD` for High/Low Match, `order` per sort (match desc / found_at desc / found_at asc), and `range` for 20-per-page with `{ count: "exact" }`. `JobFilters` is now a Client Component: 300ms-debounced `router.replace` for the text field, instant for the selects, and every change clears `page`; `applyParam` reads `window.location.search` instead of the `useSearchParams` snapshot so a pending debounce can never clobber a select change. `JobsPagination` renders real `<Link>`s preserving params (windowed page items always include the current page; disabled edges and the current page are spans). `JobsTable` gained `emptyMessage` ("No jobs match your filters." vs the first-run copy) and passes `hrefForPage` through.
- 2026-06-09: F11 lint finding — React's new `react-hooks/set-state-in-effect` rule rejected a URL→input sync effect in `JobFilters`; removed it (it could also clobber in-flight typing). The text input is local state seeded from `?q=` on mount; back/forward updates the table and selects, while the text field intentionally keeps the last typed value.
- 2026-06-09: F11 verification — lint/build green, then exercised live in the logged-in browser via Chrome MCP: typing "SynergisticIT" narrowed the table to exactly 1 row with the URL updating to `?q=SynergisticIT` (the user's reported "filter not working" flow, now functional); `?match=high` returned exactly the 3 jobs ≥ 70 (88/80/75) with the dropdown reflecting URL state; `?sort=oldest` ordered by `found_at` ascending. Pagination summary, disabled edges, and the single page chip all render with real numbers.
- 2026-06-09: F11 post-review minor fixes (all three from /review). (1) URL→input sync restored the React-sanctioned way — render-phase adjustment in `JobFilters` guarded by a `lastUrlQuery` comparison plus an `isEditing` focus flag (onFocus/onBlur): back/forward and in-app links now update the text field, while a focused user's in-flight text always wins; passes the new react-hooks lint rules. (2) A filter that sanitizes to nothing (e.g. "%%%") is clamped to zero results — `isUnsearchableQuery` skips the DB call and counts as an active filter — instead of silently matching everything. (3) A jobs read error now renders "Could not load your jobs. Refresh the page to try again." instead of the misleading first-run copy. Verified live via DOM evaluation in the logged-in browser: navigating away from `?q=%%%` via a navbar link cleared the field; `history.back()` restored `?q=%25%25%25`, the "%%%" field text, and the clamped "No jobs match your filters." row; `history.forward()` round-tripped cleanly. (Chrome-extension screenshot capture flaked mid-verification — console showed only extension messaging noise and zero React errors, so DOM checks served as the evidence.) Lint/build green.

- 2026-06-10: Built Feature 12 (Job Details Page - Full UI) matching `context/designs/job-details.png` for the authenticated detail surface. New route: `app/find-jobs/[id]/page.tsx` (Server Component, Next 16 `params` promise, `dynamic = "force-dynamic"`, `requireCurrentUser()`, user-scoped `jobs` `.maybeSingle()`, `notFound()` for missing/inaccessible rows). New components: `components/job-details/JobInfo.tsx` (header card, View Job Post, 4 info cards), `MatchScore.tsx` (AI match reasoning + matched/missing skill badges), `JobDescription.tsx` (description plus optional structured bullet sections), `CompanyResearch.tsx` (empty state + inert Research Company button; Feature 13 wires it), and `JobActions.tsx` (external Apply Now CTA). Modified `components/find-jobs/JobsTable.tsx` so company/role cells link to `/find-jobs/{job.id}` without changing the table's default visual shape. Added token utilities to `app/globals.css` for the screenshot-specific info/muted tones and imprinted the patterns in `context/ui-registry.md`.
- 2026-06-10: F12 verification - `npm run lint`, `npm run build`, and `git diff --check` pass. Production build lists `/find-jobs/[id]` as a dynamic route. Browser verification reached the expected protected-route redirect to `/login?next=%2Ffind-jobs` because the in-app browser session is not authenticated; no backend state or auth bypass was created just to screenshot the page. A logged-in visual pass should click a company/role from `/find-jobs` and compare the detail stack against `context/designs/job-details.png`.
- 2026-06-10: F12 review fix - job descriptions that end mid-sentence are source-data previews, not CSS clipping. `JobDescription` now detects saved descriptions ending in Unicode ellipsis (`U+2026`) or `...`, shows a subtle preview notice, and links directly to the original job post via the saved apply/source URL so users can reach the full posting when Adzuna's snippet is incomplete.
- 2026-06-10: F12 UI bugfix - `JobInfo` fact-card values now use `break-words` within a `min-w-0 flex-1` wrapper, so long location names remain fully visible inside the Location card instead of being truncated.
- 2026-06-10: Built Feature 13 (Company Research Agent). Installed `@browserbasehq/sdk`, `@browserbasehq/stagehand`, and `zod`; installed Browserbase `browse` CLI globally and verified `browse cloud projects list` against `.env.local` credentials (project `d6feaea0-01d8-4568-a448-d616aee28e7f`, concurrency 3). Browserbase skill setup is installed at `C:\Users\huuth\.agents\skills\browse\SKILL.md`; on Windows, run with the user npm shim first in PATH (`$env:PATH='C:\Users\huuth\AppData\Roaming\npm;' + $env:PATH; browse skills install`) to avoid the `C:\Program Files\nodejs\npx.cmd` shell quoting bug. New files: `lib/browserbase.ts` (120s company-research session create + explicit `REQUEST_RELEASE` cleanup), `lib/stagehand.ts` (Stagehand v3 configured for existing Browserbase sessions, `disableAPI: true`, Gemini model), `lib/company-research.ts` (shared dossier sanitizer/parser), `agent/research.ts` (homepage derivation from Adzuna redirect, Stagehand Zod extraction, max 3 internal sub-pages, Gemini 2.5-flash synthesis with conservative fallback), and `app/api/agent/research/route.ts` (auth/profile/job load, user-scoped save to `jobs.company_research`, PostHog `company_researched`, revalidation). Modified `components/job-details/CompanyResearch.tsx` into a Client Component with pending/error states, refresh action, and full 9-field dossier rendering. Modified `app/find-jobs/[id]/page.tsx` to read and parse `company_research`. `npm run lint` and `npm run build` pass; production build lists `/api/agent/research` as a dynamic route.
- 2026-06-10: F13 decisions - kept AI synthesis on Gemini 2.5-flash for consistency with shipped Features 07, 08, and 10 instead of adding another AI provider. Stagehand extraction also uses `google/gemini-2.5-flash` via the existing `GEMINI_API_KEY`. Because Stagehand v3 with `disableAPI: true` does not request Browserbase session release on close, the app explicitly calls Browserbase `sessions.update(...REQUEST_RELEASE...)` after `stagehand.close()`. Company homepage derivation follows the saved apply URL redirect first, ignores known ATS domains (`greenhouse.io`, `lever.co`, Workday, Ashby, iCIMS), and falls back to `https://www.{clean-company}.com`. If browser research or Gemini synthesis fails, the feature still saves a complete conservative dossier grounded in the saved job posting and profile.
- 2026-06-10: F13 post-review fixes. Added `lib/company-research-url.ts` plus `tests/company-research-url.test.ts` and `npm test` for the pure URL/source rules. Server-side redirect following is now limited to HTTPS URLs on trusted Adzuna root domains (`adzuna.com`, `adzuna.co.uk`, `adzuna.com.au`, `adzuna.ca`); user-owned rows with any other saved URL skip redirect-following and use the company-name homepage fallback. Research sources now include only pages that were actually opened and meaningfully extracted, and fallback dossiers no longer display guessed homepages or saved job URLs as sources. Verified Browserbase skill installation with the PATH workaround above.
- 2026-06-10: F13 UI review fix - `CompanyResearch` now renders the completed dossier as a responsive grid of field cards in the build-plan order (Company Overview, Tech Stack, Culture, Why This Role, Your Edge, Gaps to Address, Smart Questions, Interview Prep, Sources) instead of stacked paragraph-style sections.
- 2026-06-10: F13 review fix - improved the completed research layout with icon-led dossier cards, a full-width Tech Stack card, and lighter body typography for readability. Added `lib/company-research-tech.ts` plus tests so `techStack` is derived only from Stagehand/company-page technology evidence and explicit job-posting technology mentions; resume/profile skills and `matchedSkills` are no longer used as company tech fallback. The job-detail read path also filters stale saved `company_research.techStack` before display so older polluted dossiers stop showing resume-derived technologies.
- 2026-06-10: F13 UI ordering fix - reordered Company Research cards for applicant viewing priority: Company Overview, Tech Stack, Culture, Your Edge, Gaps to Address, Why This Role, Smart Questions, Interview Prep, Sources. This puts candidate fit before role rationale so users see their relevance before deeper interview-prep details.

- 2026-06-10: Built Feature 14 (Dashboard Page - Full UI) matching `context/designs/dashboard.png` per explicit user instruction. Rewrote `app/dashboard/page.tsx` from the F02 stub (Footer dropped — authed-shell pattern; find-jobs content width `max-w-[1280px] px-6 py-8 lg:px-0`). New components in `components/dashboard/`: `StatsBar.tsx` (4 stat cards: 284 jobs / 82% match / 35 researched / 28 this week, trend badges on the first two), `RecentActivity.tsx` (timeline list with tone dots + 1px `bg-border` connectors, design's exact 5 entries and dot colors), `ResearchActivityChart.tsx` (blue weekly bars), `JobsOverTimeChart.tsx` (accent area line, gradient fill 0.2→0), `MatchDistributionChart.tsx` (green range bars), `IncompleteProfileBanner.tsx` (white card + accent CTA to /profile). All stats/activity/chart data is mock, held in the page as typed constants and passed as props (F05/F09 pattern) so Features 15-17 swap in real data with the same prop shapes.
- 2026-06-10: F14 installed `recharts@3.8.1` (added to the code-standards approved list). Build plan F17 mandates recharts for the real charts, so the mock charts are recharts from day one — F17 becomes a data-only swap with zero visual change. Charts are Client Components; all recharts colors are CSS-variable strings (`var(--color-info)`, `var(--color-success)`, `var(--color-accent)`, grid `var(--color-border)`, ticks `var(--color-chart-axis)`) — verified at runtime that computed SVG fills resolve the variables (no hex anywhere). One-component-per-file rule splits architecture.md's sketched `AnalyticsCharts.tsx` into the three chart files above.
- 2026-06-10: F14 DEVIATIONS (design wins over build-plan text, per "exactly as shown in the png"): (1) 4th stat card is "Jobs This Week" (design + F15 wiring list), not build-plan F14's "Cover Letters Generated" (cover letters are out of scope in project-overview). (2) The bar chart beside Recent Activity is "Company Research Activity" (design + F17 + project-overview), not build-plan F14's "Resume Tailoring Activity". (3) Trend badges only on the first two stat cards; cards 3-4 carry muted captions, as drawn.
- 2026-06-10: F14 chart animation decision — `isAnimationActive={false}` on every recharts series. Live-verified failure mode with animations on: in a hidden/background tab Chrome pauses rAF, freezing the mount animation at frame zero (bars render as ~2px slivers, DOM-confirmed `visibilityState: "hidden"` with bar height 2.42px), and a force-dynamic dashboard would re-animate on every navigation anyway. Static final-state render matches the static design.
- 2026-06-10: F14 incomplete-profile banner — the design omits it (its state has a complete profile) but build-plan F14 requires it and no later feature wires it, so it ships now with the only real data read on the page: `profiles.is_complete` via `maybeSingle()`. Renders only when not `true`; read errors log `[dashboard]` and hide the banner. Mock-data note: stats/activity/charts stay mock until F15-17.
- 2026-06-10: F14 verification — `npm run lint`, `npm run build` (dashboard listed as dynamic), `npm test` 9/9, `git diff --check` clean. Live logged-in pass via Chrome MCP on the running dev server: full-page screenshots match the design section-by-section (stat values/badges, activity entries + dot tones + connectors, all three charts' shapes, axis ticks, 2:1 bottom split); DOM checks confirmed computed bar fill `rgb(97,168,255)` (= `--color-info`) and 4 connector lines at `--color-border`; console clean except known Chrome-extension messaging noise. globals.css gained `.bg-border`, `.bg-info-light`, `.bg-success-light`, `.bg-success-alt`, `.text-success-darker`.
- 2026-06-10: Built Feature 15 (Stats Bar - Real Data). `app/dashboard/page.tsx` now runs three parallel queries via `Promise.all`: the existing `profiles.is_complete` read, `jobs` `select("match_score, found_at", { count: "exact" })` scoped to the user (lightweight columns only — no jsonb fetched, per the InsForge skill's large-JSONB warning), and a count-only `.is("company_research", null).limit(1)` query. Companies Researched = total − IS-NULL count, because the documented SDK filter set (`eq/neq/gt/gte/lt/lte/like/ilike/in/is`) has no IS NOT NULL filter — subtraction uses only documented API. Pure math lives in `lib/dashboard-stats.ts` (`computeDashboardStatValues`: rounded avg over non-null scores, rolling-7-day window for Jobs This Week, researched clamped ≥ 0) with 5 new tests in `tests/dashboard-stats.test.ts`. `StatsBar` component unchanged — the page builds the same `DashboardStat[]` prop shape from real values.
- 2026-06-10: F15 decisions — the mock's "+12% / +3% vs last week" trend badges are NOT shown with real data: build-plan F15 defines exactly four values and no honest trend source, so fabricating deltas was rejected; captions are now factual ("All time", "Across all jobs", "Total researched", "New this week"). Cards 3-4 keep their design captions. Avg Match Rate renders "—" when the user has no scored jobs (0% would misread as terrible matches); on a stats query error all four values render "—" with `[dashboard]` logging instead of fake zeros.
- 2026-06-10: F15 verification — `npm test` 14/14 (5 new), lint + build green. Live logged-in dashboard shows real values 41 jobs / 50% avg / 3 researched / 41 this week (account 2 days old, so total = this-week is correct); Total Jobs Found cross-checked against the find-jobs pagination summary ("1 to 20 of 41 results") — independent query path, same number.
- 2026-06-10: Built Feature 16 (Recent Activity - Real Data). The dashboard feed now merges completed `agent_runs` ("Found X jobs for TITLE", success/green dot) with researched jobs ("Researched COMPANY", info/blue dot), newest first, capped at 5 (the design's count — keeps the card height balanced with the research chart beside it). Merge/format logic is pure in `lib/dashboard-activity.ts` (`buildRecentActivityItems`: completed_at→started_at fallback, singular "1 job" wording, "your search" fallback for blank titles, skips rows without usable timestamps/company) with 4 tests. Page queries joined the existing `Promise.all`: `agent_runs` filtered `status = "completed"` ordered by `completed_at` desc limit 5, and researched jobs via `.or("researched_at.not.is.null")` ordered by `researched_at` desc limit 5 — the or() filter tree handles IS NOT NULL (live-verified against the real backend; same SDK pathway F11 uses for or(ilike)). `RecentActivity` gained an `emptyMessage` prop so a query error shows "Could not load your recent activity…" instead of the first-run copy; errors log with `[dashboard]`.
- 2026-06-10: F16 schema addition — migration `20260610173244_add-researched-at.sql` (applied): `jobs.researched_at timestamptz`, backfilled to `found_at` for existing dossier rows (the only timestamp they have). Required because the build plan merges activity by time but F13 stored no research timestamp. `app/api/agent/research/route.ts` now stamps `researched_at` alongside `company_research` on every save, so future entries are exact. architecture.md jobs table updated. Backfill verified via CLI: 3 researched rows, 3 stamped, 41 total.
- 2026-06-10: F16 verification — `npm test` 18/18 (4 new), lint + build green. Live logged-in feed matches a CLI ground-truth query row-for-row: "Found 10 jobs for AI Developer / 3 hours ago", "Researched Texas Regional Physicians / 3 hours ago", "Found 0 jobs for AI Developmer", "Found 1 job for AI Developmer" (singular case rendered live), "Found 10 jobs for full stack Developer / 18 hours ago" — correct order, tones, and relative times.
- 2026-06-10: F14 review fix (user-reported console warnings) — every dashboard page load logged 6 dev warnings: recharts "The width(-1) and height(-1) of chart should be greater than 0" (3 charts × StrictMode double-render). Root cause: `ResponsiveContainer`'s `initialDimension` defaults to `-1×-1`, so the chart renders once before `ResizeObserver` measures (and during SSR). Fix: pass `initialDimension={{ width: 580|800|360, height: 280 }}` per chart — first-paint estimates the observer corrects on mount; also gives SSR markup real proportions. Live-verified: dashboard console now shows only the React DevTools info line and `[HMR] connected`; find-jobs and profile consoles swept clean too. Lint/build re-run green. Pattern recorded in library-docs.md (Recharts rules) and ui-registry.md.
- 2026-06-10: Built Feature 17 (Analytics Charts - PostHog Data) — the final build-plan feature. The three F14 mock charts now render real PostHog events via the PostHog Query API (HogQL): Jobs Found Over Time = `job_found` per day, last 30 days; Match Score Distribution = `matchScore` buckets across all `job_found` events (all-time); Company Research Activity = `company_researched` per day, last 7 days. New `lib/posthog-query.ts` (`queryPostHogHogQL` — POST `{POSTHOG_API_HOST}/api/projects/{POSTHOG_PROJECT_ID}/query` with `Bearer POSTHOG_PERSONAL_API_KEY`, HogQLQuery + `values` placeholders, returns rows or null on any failure) and `lib/dashboard-charts.ts` (pure `buildDailySeries`/`buildMatchDistribution`/`computeYAxis`/`hasAnyCount` + three thin fetchers). Page adds the fetchers to the existing `Promise.all`; mock constants deleted. Charts gained `emptyMessage` (RecentActivity pattern) + a `yAxis` prop (page-computed via `computeYAxis` so axes scale to real counts; type-only import keeps chart bundles clean).
- 2026-06-10: F17 decisions — (1) Querying requires a personal API key (`phx_`, scope `query:read`); the `phc_` ingestion key cannot query, and the query host `us.posthog.com` differs from the ingestion host `us.i.posthog.com`. New env: `POSTHOG_PERSONAL_API_KEY`, `POSTHOG_PROJECT_ID=425525`, `POSTHOG_API_HOST`. (2) Distribution gained a sixth `<50%` bucket (user-approved deviation from build-plan's five): live data has 23/51 events scoring below 50 because the agent saves ALL scored jobs (threshold only marks strong matches) — spec buckets would silently drop them. (3) Charts count PostHog events, not DB rows — totals may differ from the stats bar (51 events vs 41 DB jobs; events exist only since Jun 9). (4) Any query/config failure degrades to the per-chart message "Could not load chart data…" with `[posthog/query]` logging; the page never crashes. (5) `interval={0}` forces all six distribution X labels (recharts auto-skip dropped "80-90%" in the 1/3-width card).
- 2026-06-10: F17 review fixes (all /review findings) — (1) `queryPostHogHogQL` now sends `AbortSignal.timeout(8000)`: a hung (not failing) PostHog endpoint previously blocked the entire force-dynamic dashboard SSR via the shared `Promise.all`; timeouts surface through the existing catch → `null` → per-chart fallback. (2) Non-OK responses log a 300-char slice of PostHog's error body (names e.g. a missing `query:read` scope), not just the status. (3) Boundary-assertion comments added to the Query API response assertions per code-standards. (4) Time-window charts' default empty copy now states the window ("No jobs found in the last 30 days…", "No research activity in the last 7 days…") — accurate for users whose only events predate the window; the all-time distribution copy keeps "yet". (5) `DAILY_EVENT_COUNT_QUERY` takes literal-union params (`"job_found" | "company_researched"`, `30 | 7`) so the interpolated SQL template cannot receive user input — `userId` stays in HogQL `values`. (6) architecture.md's lib/ file map brought current: added `posthog-query.ts` + `dashboard-charts.ts` and the previously missing `dashboard-stats.ts` (F15) + `dashboard-activity.ts` (F16).
- 2026-06-10: F17 verification — `npm test` 26/26 (8 new in `tests/dashboard-charts.test.ts`), lint + build green. Direct Query API probe with the real key confirmed `values` placeholder substitution and returned Jun 9 = 40 / Jun 10 = 11, matching MCP `query-trends` and `execute-sql` cross-checks exactly. Live logged-in dashboard renders all three charts with real data (distribution bars 23/4/10/6/5/3 sum-verified against per-score SQL counts); console clean. Degradation live-tested: with the personal key removed, all three charts show the fallback message while stats/activity keep working; restored key brings charts back. PostHog MCP note: the MCP's default org/project was wrong ("Language App") — use `switch-organization` → org `Tom Nguyen` (019e2bb8…) + `switch-project` → `JobApplication` (425525) in future sessions.
- 2026-06-10: Brand asset update (post-build-plan polish). Rebuilt `public/logo2.png` from the user's new artwork (teal document/magnifier app icon + navy "Job Application" wordmark) to the legacy `logo.png` format — 496×168 canvas, transparent background, icon-left/wordmark-right lockup; the tagline line was dropped (matches logo.png's icon+wordmark format and would be illegible at navbar scale). Processing via one-off sharp script: border flood-fill made the icon's white background transparent while preserving the white document artwork inside it; GIMP-style white color-to-alpha cut the wordmark (letter counters stay transparent). `components/layout/Logo.tsx` now renders `/logo2.png` via `next/image` (`h-10 w-auto`) instead of the CSS-gradient mark + code-rendered text; dead `.brand-logo-mark`, `--color-logo-gradient-end`, and `--radius-logo` removed from `globals.css`. `app/favicon.ico` regenerated from the icon mark alone (16/32/48/256 PNG-encoded ICO entries). Logo renders in Navbar, Footer, and the login gradient panel.
- 2026-06-10: Logo wordmark orientation fix (user feedback: stacked two-line wordmark "not good"). Rebuilt `public/logo2.png` as a single-line lockup — split the stacked "Job"/"Application" crop at its largest fully-transparent row run, normalized both words to equal cap height (Job has no descenders so its box height is its cap; Application's box was scaled by 1.215 to account for p/p descenders), cap-top aligned on one baseline with 0.32-cap word spacing. New canvas 894×168 (content-hugging at 168 tall, like logo.png); `Logo.tsx` intrinsics updated to `width={894}`. Favicon unchanged (same icon mark).
- 2026-06-10: Homepage hero preview now shows the real app. Replaced `public/images/dashboard-demo.png` (was the design kit's JobPilot mockup with fake numbers) with a screenshot of the live logged-in `/dashboard` (real stats 41/50%/3/41, real activity feed, real charts, new logo in navbar) composed into a recreated browser frame matching the original asset's style — dark bezel, traffic-light dots, URL pill `jobapplication.app/dashboard`, 1920×969 (1.982 aspect, same as the original 4788×2416). Capture method: magenta viewport marker injected in the page + Win32 `PrintWindow` (`PW_RENDERFULLCONTENT`) on the Chrome window so capture works without foregrounding (Teams kept stealing focus and one full-screen grab caught the wrong window — that file was restored from git immediately); Next dev-tools indicator hidden during capture. `Hero.tsx` intrinsics updated to `width={1920} height={969}`. Regenerating later = re-run the same capture+compose flow against the live dashboard.
- 2026-06-10: Removed the last user-visible "JobPilot" branding. The homepage agent-log mockup (`public/images/agnet-log.png`, design-kit asset with "[SYSTEM] Initializing JobPilot Agent..." baked into pixels) was rebuilt as a faithful SVG render at the same 2144×1656 size — same window chrome/dots/colors/log lines, line 1 now "[SYSTEM] Initializing JobApplication" with "Agent..." wrapped (the longer name doesn't fit one line; wrap mirrors line 4's existing style). Saved as `public/images/agent-log2.png` (new filename per the cache rule), old file deleted, `ApplicationConfidence.tsx` src updated. `jobs-lists.png` audited clean (no branding). Remaining non-visible "JobPilot" strings: `components/PostHogProvider.tsx` localStorage key `jobpilot.posthog.userId` (left intentionally — renaming would orphan stored identity-sync state for zero user-visible gain) and historical mentions in context docs.
- 2026-06-10: /review fix pass — all 5 minor findings resolved. (1) Mockup wording aligned with the spaced brand: new `public/images/agent-log3.png` says "Job Application Agent..."; `agent-log2.png` deleted; `ApplicationConfidence.tsx` src updated. (2) Legacy `public/logo.png` deleted (unreferenced since the Logo.tsx image swap). (3) Untracked `public/.DS_Store` deleted (`.gitignore` already lists `.DS_Store`). (4) Asset weights: `logo2.png` palette-quantized in place 153KB→53KB (pixels unchanged, so the filename-bump cache rule doesn't apply); `favicon.ico` rebuilt with 16/32/48 entries only, 140KB→11KB. (5) PostHog sessionStorage key renamed `jobpilot.posthog.userId` → `jobapplication.posthog.userId` — sessionStorage is per-tab and short-lived; worst case is one redundant idempotent identify in already-open tabs. Lint/build green; live homepage HTML re-verified (agent-log3 served, zero stale/JobPilot strings in code besides historical docs).
- 2026-06-10: Hero preview cache fix — the user still saw the old mockup because the replacement kept the same filename/URL, so the browser served its cached copy (server-side was verified correct: disk SHA1 = served SHA1, homepage HTML and optimizer both fine). Renamed the asset to `public/images/dashboard-demo2.png` and updated `Hero.tsx` src — a new URL busts browser + optimizer caches deterministically. Rule for future asset swaps in `public/`: never replace an image in place and keep its name; bump the filename.
- 2026-06-11: Production login outage root-caused (first Vercel deploy, project created this day). Every "Continue with Google/GitHub" on `https://job-application.tomnguyen.me` bounced back to `/login?error=oauth_init_failed`; Vercel runtime logs show `[auth/oauth-init]` errors on each POST /login. Diagnosis: the login page's provider buttons render from a live `getPublicAuthConfig()` SSR call — production renders both buttons, so the InsForge URL + anon key on Vercel are valid and the backend is reachable. The failure is isolated to `signInWithOAuth`, whose `redirectTo` (`https://job-application.tomnguyen.me/api/auth/callback`) was rejected because `insforge.toml` `allowed_redirect_urls` only allowlisted the two localhost callbacks. Fix: added the custom-domain and default `*.vercel.app` production callback URLs to `insforge.toml`. The toml is config-as-code — it changes nothing until applied: run `npx @insforge/cli config plan` then `npx @insforge/cli config apply` from a machine with `.insforge/project.json` (not available in remote sessions), or set the same URLs in the InsForge dashboard auth settings. Also verify Vercel Production env `NEXT_PUBLIC_APP_URL=https://job-application.tomnguyen.me` — `NEXT_PUBLIC_*` is inlined at build time, so changing it requires a redeploy. Preview deployments (random `*.vercel.app` URLs) are not allowlisted; OAuth init will fail there unless their URLs are added too.
- 2026-06-12: CompletionIndicator hardening (user report after the extraction fix: "progress bar wrongly indicating" at 92%). Investigation: the math was correct — 12 tracked fields, 11 filled, round(11/12×100)=92, the single JOB TITLES chip matched the saved profile (the Abigail Hall test resume has no titles-seeking section and the extractor never invents data). The hide-at-100% rule existed only at the call site (`app/profile/page.tsx` renders the card when `missingFields.length > 0`); the component itself still carried an unreachable green "Profile complete" branch and would render at 100% if any caller forgot the guard. Fix: `CompletionIndicator` now returns `null` when `missingFields` is empty (dead complete-branch removed), making the guarantee intrinsic. New `tests/profile-completion.test.ts` pins 12/12 → 100%/complete, 11/12 → 92% + ["Job Titles"], null render at complete, card render while missing. 39/39 tests, lint, typecheck, build green.
- 2026-06-12: F07 extraction fix part 2 — local retest still showed "data didn't extract"; session replay proved the round trip completed (user clicked Extract, then clicked into the still-empty Full Name field 15s later) with zero console errors, and the View link (same `storage.download` call) serves the PDF fine, so the payload was good. Remaining silent failure points removed: `ProfilePageContent` no longer gates apply behind `window.confirm` (native dialogs are invisible to replay and silently discard the data when cancelled/suppressed — clicking Extract IS the consent); `ResumeUpload` shows an explicit green "Resume data applied…" line on success; an empty extraction now 422s server-side ("No profile data could be read from this resume PDF.") and errors client-side instead of no-opping; the route logs extracted field names for terminal diagnosis. Lint, 33/33 tests, build green.
- 2026-06-12: F07 extraction fix (user report: "Extract from Resume not working properly" on an Enhancv test resume). Production evidence: POST /api/resume/extract returned 200, but session replay showed ~2 minutes of dead spinner between the Extract click (08:19:13 CDT) and the next page activity — `agent/extractor.ts` called Gemini with NO config, so 2.5-flash's default dynamic thinking dominated latency (the exact F10 matcher finding, now minutes for a full PDF), plus default temperature 1.0 and no JSON mode (both violating library-docs Gemini rules). Fix: `config: { temperature: 0.3, responseMimeType: "application/json", thinkingConfig: { thinkingBudget: 0 } }`. Also fixed resume-specific data traps this test PDF exercises: prompt now says to prefer an explicitly stated experience total and never sum overlapping date ranges (this resume has two near-fully-overlapping roles — naive summation gives ~10y/lead vs the stated 7y/senior), to set `currentlyWorking: true` for Present-ended roles (the example shape had hardcoded `false`), and to return null for bare placeholder links ("linkedin.com" / "https://github.com" with no personal path — Enhancv template artifacts). Sanitizer hardened as deterministic backstop: `cleanProfileUrl` drops placeholder/junk URLs and prefixes https:// (a schemeless value in the form's type="url" inputs silently blocks Save Profile), Present-like endDate forces currentlyWorking + empty endDate, enum casing normalized (previously "Senior" was silently dropped and a non-enum degree was corrupted to forced "high_school"), yearsExperience rounded to a non-negative integer string. `sanitizeExtractedProfile` exported + 7 new tests in `tests/extractor-sanitize.test.ts`. `npm test` 33/33, lint green, build green (env-dependent prerender needs NEXT_PUBLIC_INSFORGE_* present).
- 2026-06-11: Login outage CLOSED — config applied and verified end-to-end. `npx @insforge/cli config plan` showed exactly one change (adding the two production callback URLs; no other drift, nothing skipped) and `config apply` succeeded. Verified `NEXT_PUBLIC_APP_URL` is set correctly on Vercel without env access: initiating OAuth from the `*.vercel.app` production domain produced a Google URL whose `state` JWT payload carries `redirectUri: https://job-application.tomnguyen.me/api/auth/callback` — the configured origin overriding the request host proves the env var (debug trick: InsForge's `state` param is base64 JSON wrapping a JWT that exposes the effective `redirectTo`). Full retest on the custom domain: Sign out → Continue with Google → account chooser → landed on /dashboard authenticated. Cross-domain caveat for future tests: the PKCE verifier cookie is set on the initiating host, so a login started on `*.vercel.app` would spuriously fail at the custom-domain callback (`NEXT_PUBLIC_APP_URL` pins the callback to the custom domain) — test initiation and completion on the same host.

- 2026-06-12: Documented Feature 18 (Job-Tailored Resume Agent) as a doc-first requirement update before implementation. Scope: job details page only, separate from the Profile page Generate Resume flow; uses saved job details + saved profile to generate an ATS-simple tailored PDF; stores the generated file in temporary InsForge storage with a `tailored_resumes` metadata row; expires after 15 days via scheduled cleanup; never replaces the active base resume at `resumes/{user_id}/resume.pdf`.
- 2026-06-12: Built Feature 18 (Job-Tailored Resume Agent). New files: `agent/tailored-resume.ts` (Gemini 2.5 Flash prompt + sanitizer, missingSkills treated as forbidden gaps), `lib/tailored-resume.ts` (15-day TTL, storage key, latest/expired helpers), `components/job-details/TailoredResumeAction.tsx` (idle/generating/ready/expired/error UI near Apply Now), `app/api/jobs/[id]/tailored-resume/route.ts` (auth/profile/job checks, Gemini rewrite, react-pdf render, private storage upload, metadata insert, previous same-user/job cleanup), `app/api/jobs/[id]/tailored-resume/download/route.ts` (latest unexpired private PDF stream), `app/api/jobs/[id]/tailored-resume/TailoredResumeDocument.tsx` (single-column ATS PDF), `functions/cleanup-tailored-resumes.js` (daily expired storage-first cleanup), migration `20260612183000_create-tailored-resumes.sql`, and tests in `tests/tailored-resume*.test.ts`.
- 2026-06-12: F18 backend applied live: migration created `tailored_resumes` with owner-scoped RLS, private `tailored-resumes` bucket created, `cleanup-tailored-resumes` InsForge function deployed active, manual function invoke returned success with 0 expired rows, and daily schedule `Cleanup Tailored Resumes` created for `0 3 * * *`. Profile page Generate Resume remains unchanged and continues to target `resumes/{user_id}/resume.pdf`.
- 2026-06-12: F18 review follow-up: tailored resume metadata now persists both `storage_key` and authenticated `storage_url`; cleanup requires an authorized bearer token before using admin credentials and preserves metadata rows when storage deletion fails so expired files can be retried.
- 2026-06-13: Mobile navbar review fix - `NavLinks` no longer hides below `md`; it wraps into a second top-navbar row on mobile using the existing Dashboard / Find Jobs / Profile links. `Navbar` now allows wrapping and uses a smaller mobile logo so the brand/action row fits without adding a drawer or sidebar. `Logo` accepts an optional className for that responsive Navbar sizing.
- 2026-06-13: F18 tailored resume distinction fix - verification showed the regular and tailored PDFs were separate artifacts but too visually similar when a saved Adzuna job had no structured requirements or responsibilities. The tailored agent prompt now feeds bounded `visibleJobSignals` and requires visibly job-specific wording from the target role/about-role/matched skills, while the tailored PDF body includes an ATS-safe `Target Role: {title} at {company}` line. Profile page Generate Resume remains unchanged.
- 2026-06-13: Audit fix pass - removed generated log files from the repo root and ignored `*.log`; raised Next.js Server Action body size to 6 MB so the documented 5 MB resume upload can reach `saveResume`; documented tailored-resume cleanup env vars; wired `profiles.cover_letter_tone` through the Profile form/save path without changing completion rules; extracted tailored-resume download resolution into `lib/tailored-resume-download.ts` with route-behavior tests for unauthorized, inaccessible, expired, and latest-unexpired cases.
- 2026-06-13: Website modernization pass started from `context/website-modernization-plan.md` through authenticated workspace polish without adding payment, admin, team, billing, or subscription features. Added semantic light/dark theme support in `app/globals.css`, pre-hydration theme application in `app/layout.tsx`, and `components/layout/ThemeToggle.tsx` in the existing top navbar. Modernized homepage/login copy and layout, added grounded homepage sections (`OutcomeStrip`, `TrustSection`), removed the fake testimonial component, polished dashboard with `TodayWorkspace`, restored visible `Jobs by Adzuna` credit plus mobile job cards, reordered job details into a decision flow, and added `CareerReadinessSummary` to Profile while preserving existing route labels, PostHog event names, auth gates, discovery/research/resume/tailored-resume endpoints, and Profile form field order.
- 2026-06-13: Progress realignment verified the old build-plan phases are complete through Phase 6 / Feature 18. The live tree contains the tailored-resume generation and download routes, storage metadata helpers, cleanup function, migrations, and tailored-resume route/agent tests. The current product-development track is now website modernization, with Phases 1-4 implemented. Fixed the dark-mode primary CTA contrast review finding by making dark-mode `--color-accent-foreground` a dark teal for bright accent backgrounds and moving overlay text to `--color-overlay-foreground`. Next new development phase: Modernization Phase 5 - Engagement Features Without Billing.
- 2026-06-13: Navbar signout/theme visual fix - `components/layout/SignOutButton.tsx` now uses the theme-aware secondary surface pattern (`bg-surface`, `border-border`, `text-text-primary`) instead of the overlay button pattern, so the authenticated navbar action has a light surface in light mode and a dark surface in dark mode while preserving the existing PostHog reset and `signOut` server action. Also moved dark theme token overrides out of the losing cascade layer in `app/globals.css` and made `ThemeToggle` notify React after applying the preferred theme so the icon and accessible label match the active theme after hydration.
- 2026-06-13: Tailored resume generation fix - local logs showed the job-detail tailored resume route was returning 422 because Gemini included a missing skill in the professional summary and `sanitizeTailoredResumeContent` rejected the whole response. The agent now requests JSON mode and replaces unsafe summaries with a deterministic profile/job fallback that excludes missing skills, while still filtering unsafe bullets and preserving the Profile page base resume flow.
- 2026-06-13: Website Modernization Phase 5 complete without billing, payments, admin, team, or plan-management changes. Resolved the saved ThemeToggle caveat by making the button label neutral and switching visible icons with `data-theme` CSS before hydration. Added pure engagement helpers in `lib/engagement-insights.ts` plus tests in `tests/engagement-insights.test.ts`. Dashboard now shows data-derived Today actions from profile completeness, resume availability, saved jobs, company research status, and tailored-resume freshness; it also shows repeated missing-skill insights from saved jobs. Job details now includes `InterviewPrepExpansion`, turning existing company research, match strengths, and gap skills into interview focus areas, lead-with points, gap framing, and questions to ask.
- 2026-06-13: Phase 5 review findings fixed - dashboard engagement error handling now separates profile, engagement-job, and tailored-resume read failures. Today actions no longer guess profile completion or resume readiness when profile/tailored-resume data is unavailable, and skill-gap insights remain visible when jobs data succeeds even if tailored-resume reads fail.
- 2026-06-14: Website Modernization Phase 6 readiness audit started as docs-only work. No payment, subscription, billing, admin, team, pricing, plan-management, route, schema, or UI implementation was added. The linked InsForge project is `JobApplication`, but `npx @insforge/cli payments stripe status --json` reports `Payments are not available on this backend`, so Phase 6 implementation remains gated on explicit user approval and backend enablement. Recorded the current CLI namespace (`payments stripe`), installed SDK payments method shape from `node_modules/@insforge/sdk/dist/*.d.ts`, and Stripe webhook/fulfillment requirements in `context/website-modernization-plan.md` and `context/library-docs.md`.
- 2026-06-14: Drafted `context/stripe-saas-integration-plan.md` as the separate Phase 6S review plan. Provider direction is Stripe, with InsForge payments preferred once backend payments are enabled. The plan proposes single-user billing only, Free + Pro launch tiers, app-owned `user_entitlements`, append-only `usage_ledger`, webhook idempotency, quota enforcement for expensive AI/Browserbase/resume operations, hosted checkout/customer portal surfaces, and a staged implementation sequence. This is still docs-only; no payment, subscription, billing, admin, team, route, schema, UI, or plan-limit implementation was added.
- 2026-06-29: Implemented free multi-source job discovery. Added provider-neutral `JobSourceProvider` / `NormalizedJobPosting` types, provider adapters for Adzuna, Remotive, USAJOBS, and configured Greenhouse/Lever/Ashby ATS boards, plus `agent/job-discovery.ts` orchestration for source-level outcomes, partial failure tolerance, provider/id + canonical URL dedupe, score-limit enforcement from remaining `job_match_score` quota, Gemini scoring, and normalized saves. Added and applied InsForge migration `20260629143000_add-job-source-provider-fields.sql` for provider columns, posting date, and bounded metadata while preserving `jobs.source = 'search'`. Find Jobs now shows source badges, dynamic attribution, and per-source run breakdowns; job details shows the source badge in the header. Added env examples and tests for normalization, config parsing, source failure handling, dedupe, and quota-limited scoring.

---

## Notes

- Homepage uses Inter via `next/font/google` in `app/layout.tsx`, matching `ui-rules.md`.
- Added explicit token utility aliases in `app/globals.css` for project color classes such as `text-accent-foreground` and `text-text-dark`, because the browser render did not reliably resolve every multi-part Tailwind v4 token class from `@theme` alone.
- Local runtime env values were written to ignored `.env.local` for `NEXT_PUBLIC_INSFORGE_URL`, `NEXT_PUBLIC_INSFORGE_ANON_KEY`, and `NEXT_PUBLIC_APP_URL`.
- Auth uses Next.js 16 `proxy.ts`; do not add `middleware.ts` for this project.
- Local runtime env includes `NEXT_PUBLIC_POSTHOG_KEY` and `NEXT_PUBLIC_POSTHOG_HOST`; helpers no-op with development warnings when either value is absent.
- Database schema is managed via InsForge CLI migrations in `migrations/`. Create with `npx @insforge/cli db migrations new <name>`, apply with `npx @insforge/cli db migrations up --all`. Migrations run inside a backend-managed transaction — never put `BEGIN`/`COMMIT` in a migration file. FK to users via `auth.users(id)`; use `auth.uid()` in RLS.
- `ui-registry.md` was intentionally not updated for Feature 04 — it is a backend-only feature with no UI component to register.
- 2026-06-09: Post-F06 polish: `CompletionIndicator` now hides entirely when the profile is 100% complete (`missingFields.length === 0` check in `app/profile/page.tsx`). Added `.text-success` and `.bg-success-lightest` hand-written utilities to `app/globals.css` following the existing token-utility pattern.
- Feature 04 RLS is structurally verified (correct `USING`/`WITH CHECK` on all 16 policies). It will be functionally exercised when the app first writes as a real authenticated user in Feature 06 — the InsForge server client uses the user's cookie session, so RLS applies (the admin CLI key bypasses RLS).
- KNOWN GAP (out of scope for Feature 05): the design shows the active nav item ("Profile") highlighted in `text-accent`, but the shared `components/layout/Navbar.tsx` (built in Features 01–02) renders all links with `text-text-dark` and has no active-route state. Adding active highlighting is a shared-Navbar change affecting every page — deferred so Feature 05 stays scoped to the profile page. RESOLVED in Feature 09 via `components/layout/NavLinks.tsx`.
- Feature 05 visual check note: `/profile` is auth-gated (`requireCurrentUser` redirects to `/login`), so it can't be screenshotted without a logged-in session. Verified via `npm run build`/`lint` and a section-by-section review against `profile.png`; a live visual pass should be done after logging in (or during `/review` before demo).
- 2026-06-09: Built Feature 07 (AI Profile Extraction from Resume). DEVIATION from build plan: used Gemini multimodal (`@google/genai`, model `gemini-2.5-flash`) instead of the earlier pdf-parse text-extraction plan — PDF sent as `inlineData` directly to Gemini, eliminating the pdf-parse dependency. Do NOT use `@google/generative-ai` — that package uses the deprecated v1beta endpoint and returns 404 for all current models. New files: `agent/extractor.ts` (Gemini extraction + sanitize), `app/api/resume/extract/route.ts` (POST: downloads PDF from InsForge storage, calls extractor, returns `Partial<Profile>`), `components/profile/ProfilePageContent.tsx` (client wrapper holding `mergedProfile` state and `extractionKey`). Modified: `app/profile/page.tsx` (uses ProfilePageContent instead of direct ResumeUpload + ProfileForm), `components/profile/ResumeUpload.tsx` (added `onExtract` prop, Extract button with loading state, calls `/api/resume/extract`). State sharing: `ProfilePageContent` holds extracted state; when extraction completes it merges non-null fields into a new `mergedProfile` and increments `extractionKey` — ProfileForm remounts with the key change, picking up new `defaultValues` and `useState` initializers from the merged profile. User still saves manually after reviewing. `npm run build` passes.
- 2026-06-26: Implemented the complete Stripe SaaS Integration Plan (Phases 6S.1 through 6S.8) for the JobApplication project. Created SQL migration for `user_entitlements`, `usage_ledger`, and `billing_webhook_events` with owner-scoped RLS policies. Protected expensive routes with server-side quota checks. Implemented `PlanSummary`, `UsageMeter`, and `BillingActions` components on the Dashboard and Profile page. Created public route `/pricing` with price comparison grid and login/upgrade actions. Integrated Stripe Checkout, Customer Portal, and webhook fulfillment endpoints via the InsForge Payments SDK. Configured fallback graceful degradation when payments are disabled on the backend. All tests, linter checks, and Next.js production builds compile and run successfully.

